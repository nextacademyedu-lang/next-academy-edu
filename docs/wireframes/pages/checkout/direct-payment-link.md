# Wireframe: Direct Payment Link (`/pay/:code`)

- Specific landing page skipping the normal catalog.
- Hero Banner customized by admin.
- Locked Order Summary card (price and plan are pre-set by admin link).
- Only shows Payment Details step.