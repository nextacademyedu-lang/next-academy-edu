# Session Log: 2026-03-22 — Session 19

## Summary

Implemented a targeted stabilization pass for:
- Twenty CRM compatibility with default `opportunities` pipeline flow,
- admin access false negatives (`You are not allowed to perform this action`),
- preview support for `Popups` and `Announcement Bars`.

## Started At

01:06 Cairo time

## Completed Tasks

- CRM pipeline compatibility:
  - removed skip logic that disabled deal sync when deals resource path was `opportunities`.
  - added opportunities-safe mapping payloads and local-id based upsert path.
  - enabled booking/payment/consultation event sync to create/update opportunities.
- Admin access reliability:
  - added centralized `isAdminUser(...)` helper in access-control.
  - helper supports `PAYLOAD_ADMIN_EMAIL` fallback when role sync lags.
  - applied helper to collection access checks and `Users` admin gate.
- Marketing preview:
  - added collection admin preview URLs for `popups` and `announcement-bars`.
  - added secure preview API params:
    - `/api/popups/active?previewPopupId=...`
    - `/api/announcement-bars/active?previewAnnouncementId=...`
  - enforced admin-only preview access.
  - wired frontend components to consume preview params and render selected item.
- Announcement bar runtime alignment:
  - updated frontend mapper to render collection schema fields (`messages`, `appearance`, `behavior`, `countdown`).

## Files Changed

- `src/lib/crm/mappers.ts`
- `src/lib/crm/service.ts`
- `src/lib/access-control.ts`
- `src/collections/Users.ts`
- `src/collections/Popups.ts`
- `src/collections/AnnouncementBars.ts`
- `src/app/api/popups/active/route.ts`
- `src/app/api/announcement-bars/active/route.ts`
- `src/components/marketing/popup-manager.tsx`
- `src/components/layout/announcement-bar.tsx`
- `docs/logs/changelog.md`
- `docs/logs/tasks.md`
- `docs/sessions/2026-03-22-01-06-session-19.md`

## Verification

- `pnpm.cmd exec tsc --noEmit` ✅

## Required Next Steps

1. Deploy latest commit to production.
2. Validate admin CRUD on:
   - Popups
   - Announcement Bars
3. Validate preview links from admin UI.
4. Trigger `/api/cron/crm-sync` and verify opportunities are created/updated in Twenty.

## Ended At

01:06 Cairo time

