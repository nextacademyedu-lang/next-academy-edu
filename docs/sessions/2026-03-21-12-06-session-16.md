# Session Log: 2026-03-21 — Session 16

## Summary

Resolved production schema-application gap causing runtime 500s after successful Coolify deploys.
Root cause was Payload Postgres production behavior: `push` does not run in production unless `prodMigrations` are explicitly wired.

## Started At

12:06 Cairo time

## Completed Tasks

- Reviewed production runtime logs:
  - `docs/logs/coolify/logs.txt`
- Confirmed missing schema elements in DB:
  - `programs.featured_priority` column
  - `announcement_bars`, `popups`, `upcoming_events_config`, `crm_sync_events` relations
- Generated new migration from current schema:
  - `src/migrations/20260321_100401.ts`
  - `src/migrations/20260321_100401.json`
- Verified migration registration:
  - `src/migrations/index.ts`
- Fixed runtime production migration execution:
  - updated `src/payload.config.ts` to include:
    - `prodMigrations: migrations`
    - kept `push: true` for non-production
- Ran TypeScript check:
  - `node_modules\\.bin\\tsc --noEmit` passed

## Findings

1. Successful image build/deploy does not guarantee DB schema compatibility.
2. Payload Postgres behavior is environment-sensitive:
   - non-production: schema push may run
   - production: relies on configured `prodMigrations`
3. Missing migration wiring caused application code to query non-existent DB structures.

## Required Next Steps

1. Deploy latest commit on Coolify.
2. Re-test these endpoints after deployment:
   - `/api/programs`
   - `/api/home/featured-programs`
   - `/api/announcement-bars/active`
   - `/api/popups/active`
   - `/api/upcoming-events`
   - `/api/cron/crm-sync`
3. Rotate exposed production secrets pasted in logs/chat and remove plaintext values from tracked docs.
4. Re-validate Paymob integration IDs against the currently active merchant API key/account.

## Ended At

12:06 Cairo time
