# Session Log: 2026-03-29 — Session 26 (Phase 2.1 Security Hotfixes)

## Summary

Applied a focused security hotfix batch to close critical/high audit gaps in RBAC and API endpoint protection without changing product flow.

## Scope Completed

1. Protected seed endpoints with an explicit env kill-switch and CRON bearer authentication.
2. Restricted `user-profiles` updates to owner/admin only.
3. Restricted `blog-posts` create/update/delete to admin only.
4. Restricted `notifications` updates to owner/admin only.
5. Added CSRF protection to `POST /api/bulk-seats/allocate`.

## Key Technical Changes

- Seed endpoint hardening:
  - `src/app/api/seed-instructors/route.ts`
  - `src/app/api/seed-partners/route.ts`
- RBAC hardening:
  - `src/collections/UserProfiles.ts`
  - `src/collections/BlogPosts.ts`
  - `src/collections/Notifications.ts`
- CSRF hardening:
  - `src/app/api/bulk-seats/allocate/route.ts`

## Verification

- TypeScript check passed:
  - `cmd /c pnpm tsc --noEmit`
- Local Windows build note:
  - `cmd /c pnpm build` completed compile/types/static pages generation, then failed at standalone symlink copy with `EPERM` (environment limitation on Windows, not a code/type failure).

## Deployment Note

- New env toggle required for seed routes:
  - `ENABLE_SEED_ENDPOINTS` (default should remain disabled in production)

## Commit Reference

- Security hotfix commit: pending in this session
- Logs update commit: pending in this session
