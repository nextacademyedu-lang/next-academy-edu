# Session Log: 2026-03-22 — Session 21

## Summary

Fixed recurring admin access regression causing `You are not allowed to perform this action` with `403` on user update endpoints.

## Started At

14:10 Cairo time

## Completed Tasks

- Hardened access-control admin lookup:
  - normalize `req.user.id` before lookup
  - fallback query path if `findByID` fails
- Updated login cookie strategy to reduce stale-cookie precedence:
  - set host-only `payload-token`
  - set root-domain cookie for `.nextacademyedu.com` when applicable
- Added configured admin pin in `Users` collection:
  - configured admin email(s) always forced to `role=admin`
  - configured admin email(s) always forced to `emailVerified=true`
- Updated logs and tasks documentation.

## Files Changed

- `src/lib/access-control.ts`
- `src/app/api/users/login/route.ts`
- `src/collections/Users.ts`
- `docs/logs/changelog.md`
- `docs/logs/tasks.md`
- `docs/sessions/2026-03-22-14-14-session-21.md`

## Verification

- `pnpm.cmd exec tsc --noEmit` ✅

## Required Next Steps

1. Deploy latest commit.
2. Clear browser cookies for `nextacademyedu.com` and `www.nextacademyedu.com` once.
3. Login again with admin account and verify editing any user no longer returns `403`.

## Ended At

14:14 Cairo time
